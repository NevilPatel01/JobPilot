# secrets — never commit filled env files (repo is public)
.env
.env.*
!.env.example
!.env.local.example
backend/.env
frontend/.env.local
deploy/linode/env/backend.env
deploy/linode/env/frontend.env.local
# TLS keys / certs if generated locally
*.pem
*.key
deploy/linode/nginx/*.conf
!deploy/linode/nginx/*.template
!deploy/linode/nginx/jobpilot-http.conf

# private planning docs (local only)
docs/

# dependencies & builds
node_modules/
frontend/.next/
__pycache__/
*.pyc
.venv/
venv/
backend/.bin/
*.egg-info/
dist/
build/

# testing & coverage
.pytest_cache/
.coverage
htmlcov/

# OS / IDE (keep .cursor/ tracked)
.DS_Store
.idea/
*.swp
*.swo

# logs
*.log
npm-debug.log*

# playwright
playwright-report/
test-results/
